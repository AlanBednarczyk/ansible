---
  # Play1 - ping
  - name: ping
    hosts:
      - simos24_all
      - simos24_stc
      - simos24_rstc
    become: yes
    become_user: root
    tasks:

      
      - name: Ping
        command:
          cmd: ping -c 1 127.0.0.1
