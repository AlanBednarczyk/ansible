---
  # Restart roip-platform
  - name: Restart roip-platform
    hosts:
      - simos24_all
      - simos24_stc
      - simos24_rstc
    become: yes
    become_user: root
    tasks:

      - name: Restart roip-platform
        command:
          cmd: systemctl restart roip-platform
